[project]
name = "real-time-log-viewer-server"
version = "0.1.0"
description = "FastAPI WebSocket log streaming backend."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.0",
    "uvicorn[standard]>=0.45.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.11",
]
