[project]
name = "real-time-log-viewer"
version = "0.1.0"
description = "A lightweight demo app."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.0",
    "uvicorn[standard]>=0.45.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.11",
]
